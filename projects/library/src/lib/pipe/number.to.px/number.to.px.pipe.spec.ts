import { NumberToPxPipe } from './number.to.px.pipe';

describe('NumberToPxPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToPxPipe();
    expect(pipe).toBeTruthy();
  });
});
